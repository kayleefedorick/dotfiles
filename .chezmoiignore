# Machine-state files, not user config.
# These are auto-regenerated by their host applications and
# should not be version-controlled.

# VSCodium auto-manages this on every extension install.
.vscode-oss/extensions/extensions.json

# FreeCAD's recent-files / window-state preferences file;
# contains absolute paths and Ondsel-Lens project IDs.
.var/app/org.freecad.FreeCAD/config/FreeCAD/user.cfg
